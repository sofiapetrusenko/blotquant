# Runtime dependencies actually imported by this repository.
#
# numpy is pinned EXACTLY: the committed synthetic gold set under data/ is a frozen
# artifact whose pixel values are defined by numpy's legacy RandomState stream and
# float64 arithmetic. A numpy upgrade must be a deliberate, reviewed act (bump
# SYNTH_VERSION, regenerate, add a break marker in evals/history.md).
numpy==2.4.6

# Lower-bounded: used only for lossless/lossy container encoding, not for defining
# canonical pixel values.
tifffile>=2024.1.30          # 8/16-bit TIFF writing and reading
opencv-python-headless>=4.9  # PNG (8/16-bit) and JPEG q75 encoding/decoding
jsonschema>=4.20             # Draft 2020-12 validation of ground truth in tests and CI

# Added in Phase 1, for pipeline/ and its config.
PyYAML>=6.0                  # parses configs/*.yaml, the pipeline's only source of parameters
scikit-image>=0.22           # rolling-ball background (skimage.restoration.rolling_ball)
scipy>=1.11                  # local-median background and 1D peak finding for detection

# Added in Phase 4a, for api/. No imaging dependency is added: the display derivative is
# encoded with opencv-python-headless, which is already here.
fastapi>=0.110               # the HTTP layer: POST /analyze and GET /results/{id}
uvicorn>=0.27                # ASGI server 'python -m api' runs the app on; Phase 4b deploys it
python-multipart>=0.0.9      # multipart/form-data parsing, which FastAPI requires for UploadFile
httpx>=0.27                  # TEST-ONLY: the transport starlette's TestClient runs on. CI
                             # installs from this file and then runs pytest, so it belongs here.
